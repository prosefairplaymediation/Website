# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
# wrangler's local secret file. The Twilio and
# Resend credentials go here when running the Worker locally; in production
# they are Worker secrets. It was not ignored, which is one careless
# `git add -A` away from publishing live credentials.
.dev.vars

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# client-facing handoff document — contains personal account emails, not for the repo
DELIVERABLES.md
